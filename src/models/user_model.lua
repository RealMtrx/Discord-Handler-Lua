local UserModel = {}

function UserModel.find(user_id)
  -- Stub: requires MongoDB client
  return nil
end

function UserModel.upsert(user_id, data)
  -- Stub: requires MongoDB client
end

return UserModel
